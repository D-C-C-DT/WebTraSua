@model WebsiteBanTraSua.Models.Product

@{
    ViewData["Title"] = "Chi tiết";
}

<h1>Thông tin sản phẩm</h1>

<div>
    @* <h4>Sản phẩm</h4> *@
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayName("Tên sản phẩm")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayName("Giá")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Price)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayName("Hình ảnh sản phẩm")
        </dt>
        <dd class="col-sm-10">
            <img src="@Model.ImageUrl" width="150px" />
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayName("Loại đồ uống")
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Category.Name)
        </dd>
    </dl>
</div>
<div>
    @* <a asp-action="Chỉnh sửa" asp-route-id="@Model?.Id">Chỉnh sửa</a> |  *@
    <a asp-action="Danh sách">Quay lại danh sách</a>
</div>
<p>
    <strong>Thích:</strong> @Model.Likes
</p>
<button class="btn btn-primary" id="like-button">Thích</button>
</div>
</div>
<div>
    @* <a asp-action="Chỉnh sửa" asp-route-id="@Model?.Id">Chỉnh sửa</a> |  *@
    <a asp-action="Danh sách">Quay lại danh sách</a>
</div>
@section Scripts {
    <script>
        $("#like-button").click(function () {
            $.ajax({
                type: "POST",
                url: "@Url.Action("Thích", "Sản phẩm")",
                data: { id: @Model.Id },
                success: function () {
                    // Cập nhật số lượt thích trên giao diện
                    $("#likes-count").text(parseInt($("#likes-count").text()) + 1);
                },
                error: function () {
                    alert("Lỗi khi thích sản phẩm.");
                }
            });
        });
    </script>
}